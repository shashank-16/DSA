#include<iostream>
#include<string>
int main()
{
    int times;
    std::cin>>times;
    while(times--)
    {
        // char* pattern;
        std::string pattern;
        std::cin>>pattern;
        // std::cout<<pattern;
        // for(char i =pattern.front();i!='\0';i++)
        // {
        //     std::cout<<i;
        // }
    }
}
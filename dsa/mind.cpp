#include<bits/stdc++.h>
void encoding(std::string encd ,std::string decod)
{
    if(encd.size()==0)
    {
        std::cout<<decod;
        return ;
    }

    for(int i=0;i<encd.size();i++)
    {
        string ch = encd.at(i);

        std::string set = encd.
    }
}
int main()
{
    std::string decode;
    std::cin>>decode;

}